# Zsh completions for Gleam

Zsh tab completions to make typing Gleam commands a breeze.

To install completions, copy the `_gleam` file from this repo to one of the
directories listed in your `$fpath` variable, this is where zsh will look for
scripts to run.
